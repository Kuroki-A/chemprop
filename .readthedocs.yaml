# Read the Docs configuration for the maintained Chemprop v1 documentation.
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: true

formats: all

python:
  install:
    - requirements: docs/requirements.txt
