# Packaging metadata and dependencies live exclusively in setup.py.
# Keeping setup.cfg tool-only prevents the two files from drifting apart.

[flake8]
max-line-length = 127
exclude = .git,__pycache__,build,dist
